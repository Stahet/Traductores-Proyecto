program {
    using
        int i,j,k;
        bool b,c;
        set s;
    in
    if(true) {
        using
            set i;
            bool b;
        in
        i = {1,2,3};
        s = 2 <+> s;
        # nótese que al lado izquierdo del `min`, `i` es int 
        # y al lado derecho es set
        for i min 2 <+> s do {
          using bool j; in          # una `i` más
          j = i ==1;
        };
    } else {
       using bool i; in
       
    };
}
