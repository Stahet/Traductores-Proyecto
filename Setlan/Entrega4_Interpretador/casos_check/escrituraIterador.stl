program {
    using
        set s;
        int i;
    in
    s = {2, 3, 1};
    for i min s do {
        println i;
        s = s ++ {i * 2}; # unión de conjuntos
        i = i + 1;
    };
    print s;
}